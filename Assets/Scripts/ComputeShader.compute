// Each #kernel tells which function to compile; you can have many kernels
#pragma kernel Tripple

RWStructuredBuffer<float3> dataBuffer;

[numthreads(16, 1, 1)]
void Tripple(uint3 id : SV_DispatchThreadID)
{
	dataBuffer[id.x] = 3 * dataBuffer[id.x];
}
